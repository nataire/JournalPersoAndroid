<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.JournalPerso">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".GestionEspace.CreerEspaceActivity"></activity>
        <activity android:name=".GestionIndicateur.ModifierIndicateurActivity" />
        <activity android:name=".GestionIndicateur.createIndicateurActivity" />
        <activity android:name=".GestionEspace.ModifyEspaceActivity" />
        <activity
            android:name=".menuActivity"
            android:label="@string/title_activity_menu"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".InscriptionActivity" />
        <activity android:name=".GestionEspace.ConsultationEspacesActivity" />
        <activity android:name=".ConnexionActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>